---
name: file-planning
description: 将工作流程转变为使用Manus风格的持久化markdown文件进行规划、进度跟踪和知识存储。适用于启动复杂任务、多步骤项目、研究任务，或当用户提及规划、组织工作、跟踪进度或需要结构化输出时使用。
---

# 文件规划

像Manus一样工作：使用持久化的markdown文件作为你的"磁盘工作内存"。

## 快速开始

在任何复杂任务**之前**：

1. 在 `.claude_tasks/task_xxxx/` 目录下创建 `task_plan.md`
2. 用复选框定义各个阶段
3. **每个阶段后更新** - 标记 [x] 并更改状态
4. **决策前阅读** - 在注意力窗口中刷新目标

## 目录结构

所有任务文件统一存放在 `.claude_tasks` 目录下：

```
.claude_tasks/
└── task_xxxx/           # 任务目录，以任务概要命名
    ├── task_plan.md     # 任务计划和进度跟踪
    ├── notes.md         # 研究笔记和发现
    └── [交付物].md      # 最终输出文件
```

## 三文件模式

对于每个非平凡任务，在任务目录下创建**三个文件**：

| 文件 | 用途 | 更新时机 |
|------|------|----------|
| `task_plan.md` | 跟踪阶段和进度 | 每个阶段后 |
| `notes.md` | 存储发现和研究内容 | 研究期间 |
| `[交付物].md` | 最终输出 | 完成时 |

## 核心工作流程

```
循环1: 创建 .claude_tasks/task_xxxx/ 目录及 task_plan.md
循环2: 研究 → 保存到 notes.md → 更新 task_plan.md
循环3: 阅读 notes.md → 创建交付物 → 更新 task_plan.md
循环4: 交付最终输出
```

### 循环详解

**每次主要操作之前：**
```bash
Read .claude_tasks/task_xxxx/task_plan.md  # 在注意力窗口中刷新目标
```

**每个阶段之后：**
```bash
Edit .claude_tasks/task_xxxx/task_plan.md  # 标记 [x]，更新状态
```

**存储信息时：**
```bash
Write .claude_tasks/task_xxxx/notes.md     # 不要塞满上下文，存入文件
```

## task_plan.md 模板

为任何复杂任务**首先**创建 `.claude_tasks/task_xxxx/task_plan.md`：

```markdown
# 任务计划：[简要描述]

## 目标
[描述最终状态的一句话]

## 阶段
- [ ] 阶段1：计划和设置
- [ ] 阶段2：研究/收集信息
- [ ] 阶段3：执行/构建
- [ ] 阶段4：审查和交付

## 关键问题
1. [需要回答的问题]
2. [需要回答的问题]

## 已做决策
- [决策]：[理由]

## 遇到的错误
- [错误]：[解决方案]

## 代码Review
（代码修改类任务填写）
- [ ] 语法和逻辑检查
- [ ] 项目规范符合性检查
- [ ] 用户需求符合性检查
- Review结论：[通过/需要修正]

## 状态
**当前处于阶段X** - [我正在做什么]
```

## notes.md 模板

在任务目录下创建 `.claude_tasks/task_xxxx/notes.md`，用于研究和发现：

```markdown
# 笔记：[主题]

## 来源

### 来源1：[名称]
- URL: [链接]
- 关键点：
  - [发现]
  - [发现]

## 综合发现

### [类别]
- [发现]
- [发现]
```

## 关键规则

### 1. 始终先创建计划和目录
永远不要在没有创建 `.claude_tasks/task_xxxx/` 目录和 `task_plan.md` 的情况下开始复杂任务。这是不可协商的。

### 2. 决策前阅读
在任何重大决策之前，阅读计划文件。这会让目标保持在你的注意力窗口中。

### 3. 行动后更新
完成任何阶段后，立即更新计划文件：
- 用 [x] 标记完成的阶段
- 更新状态部分
- 记录遇到的任何错误

### 4. 存储，不要塞满
大输出放入任务目录下的文件，而不是上下文。工作内存中只保留路径。

### 5. 记录所有错误
每个错误都应放在"遇到的错误"部分。这为未来任务积累知识。

### 6. 代码修改后必须Review
对于代码修改类任务，在所有修改完成后**必须**进行代码Review：
- 检查代码是否存在问题（语法、逻辑、潜在bug）
- 检查是否符合项目开发规范（代码风格、命名约定、目录结构）
- 检查是否符合用户需求（功能完整性、边界条件处理）
- 将Review结果记录到 `task_plan.md` 的"代码Review"部分

## 何时使用此模式

**对以下情况使用三文件模式：**
- 多步骤任务（3个以上步骤）
- 研究任务
- 构建/创建某物
- 跨越多个工具调用
- 任何需要组织的任务

**跳过用于：**
- 简单问题
- 单文件编辑
- 快速查找

## 反模式要避免

| 不要 | 改为 |
|------|------|
| 使用 TodoWrite 持久化 | 创建 `.claude_tasks/task_xxxx/task_plan.md` 文件 |
| 状态目标一次就遗忘 | 每次决策前重新阅读计划 |
| 隐藏错误并重试 | 将错误记录到计划文件 |
| 把所有内容塞入上下文 | 将大内容存储在任务目录下的文件中 |
| 立即开始执行 | **首先**创建任务目录和计划文件 |

## 高级模式

参见 [reference.md](reference.md) 了解：
- 注意力操作技术
- 错误恢复模式
- 来自Manus的上下文优化

参见 [examples.md](examples.md) 了解：
- 真实任务示例
- 复杂工作流程模式
